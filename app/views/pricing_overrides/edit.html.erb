<% title t(:edit, :model => t("pricing_override.one") + " (" + t("store_item.other") + ")") %>

<%= render 'pricing_overrides/form', form_path: send(instance_path_method,@pricing_override) %>

<%= link_to 'Back', send(path_method) %>

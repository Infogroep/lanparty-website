<%= simple_form_for(@compo, url:save_bracket_compo_path(@compo)) do |f| %>
	<%= f.input :bracket_string, as: :hidden, input_html: {value: @compo.bracket_string} %>
	<% if current_user && current_user.access_allowed?(:compo_editing) %>
		<%= f.button :submit %>
	<% end %>
<% end %>
